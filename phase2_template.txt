            <div class="phase-subcolumns">
                <!-- Sub-column 1: Ready for BUP -->
                <div class="phase-subcolumn readybup-subcolumn">
                    <div class="subcolumn-header">
                        <span>Ready for BUP</span>
                        <span class="subcolumn-count">{{ programs|selectattr('phase', 'equalto', '2')|selectattr('subcolumn', 'equalto', 'readybup')|list|length }}</span>
                    </div>

                    {% for program in programs %}
                    {% if program.phase == '2' and program.subcolumn == 'readybup' %}
                    <div class="program-card" data-portfolio="{{ program.portfolio }}">
                        <div class="program-name" style="margin-bottom: 8px;">{{ program.name[:80] }}{% if program.name|length > 80 %}...{% endif %}</div>

                        <div style="margin-bottom: 12px;">
                            <span style="display: inline-block; font-size: 13px; color: #475569; font-weight: 600; background: #dbeafe; padding: 4px 10px; border-radius: 6px;">{{ program.portfolio }}</span>
                        </div>

                        <div class="program-meta">
                            {% if program.program_manager %}
                            <div class="meta-item">
                                <span class="meta-label">PM:</span> {{ program.program_manager }}
                            </div>
                            {% endif %}
                            {% if program.arch_lead %}
                            <div class="meta-item">
                                <span class="meta-label">Arch:</span> {{ program.arch_lead }}
                            </div>
                            {% endif %}
                            {% if program.tpm_lead %}
                            <div class="meta-item">
                                <span class="meta-label">TPM:</span> {{ program.tpm_lead }}
                            </div>
                            {% endif %}
                            {% if program.ux_lead %}
                            <div class="meta-item">
                                <span class="meta-label">UX:</span> {{ program.ux_lead }}
                            </div>
                            {% endif %}
                            {% if program.cx_lead %}
                            <div class="meta-item">
                                <span class="meta-label">CX:</span> {{ program.cx_lead }}
                            </div>
                            {% endif %}
                        </div>
                    </div>
                    {% endif %}
                    {% endfor %}
                </div>

                <!-- Sub-column 2: In Progress -->
                <div class="phase-subcolumn inprogress-subcolumn">
                    <div class="subcolumn-header">
                        <span>In Progress</span>
                        <span class="subcolumn-count">{{ programs|selectattr('phase', 'equalto', '2')|selectattr('subcolumn', 'equalto', 'inprogress')|list|length }}</span>
                    </div>

                    {% for program in programs %}
                    {% if program.phase == '2' and program.subcolumn == 'inprogress' %}
                    <div class="program-card" data-portfolio="{{ program.portfolio }}">
                        <div class="program-name" style="margin-bottom: 8px;">{{ program.name[:80] }}{% if program.name|length > 80 %}...{% endif %}</div>

                        <div style="margin-bottom: 12px;">
                            <span style="display: inline-block; font-size: 13px; color: #475569; font-weight: 600; background: #dbeafe; padding: 4px 10px; border-radius: 6px;">{{ program.portfolio }}</span>
                        </div>

                        <div class="program-meta">
                            {% if program.program_manager %}
                            <div class="meta-item">
                                <span class="meta-label">PM:</span> {{ program.program_manager }}
                            </div>
                            {% endif %}
                            {% if program.arch_lead %}
                            <div class="meta-item">
                                <span class="meta-label">Arch:</span> {{ program.arch_lead }}
                            </div>
                            {% endif %}
                            {% if program.tpm_lead %}
                            <div class="meta-item">
                                <span class="meta-label">TPM:</span> {{ program.tpm_lead }}
                            </div>
                            {% endif %}
                            {% if program.ux_lead %}
                            <div class="meta-item">
                                <span class="meta-label">UX:</span> {{ program.ux_lead }}
                            </div>
                            {% endif %}
                            {% if program.cx_lead %}
                            <div class="meta-item">
                                <span class="meta-label">CX:</span> {{ program.cx_lead }}
                            </div>
                            {% endif %}
                        </div>
                    </div>
                    {% endif %}
                    {% endfor %}
                </div>
            </div>
